import styled from "styled-components";

const OrderDetailListWrapper = styled.section`
  display: flex;
`;

export const OrderDetailMenu = styled.div`
  img {
    width: 100%;
  }
`;

export default OrderDetailListWrapper;
